const Hub = artifacts.require("Hub");

describe("Hub.sol", () => {

    before(async () => {
        let hub = await Hub.new();

    });

    it("register()", async () => {
        // Do something
    });

    it("deactivate()", async () => {
        // Do something
    });

});